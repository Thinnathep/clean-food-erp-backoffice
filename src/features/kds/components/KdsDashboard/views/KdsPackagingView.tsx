import React from 'react';

export const KdsPackagingView: React.FC = () => {
  return (
    <div className="p-6 h-full flex flex-col items-center justify-center bg-slate-50">
      <h2 className="text-2xl font-bold text-slate-800 mb-2">จัดถุงเตรียมส่ง (Packing)</h2>
      <p className="text-slate-500">ฟังก์ชันสำหรับเตรียมจัดส่งจะอยู่ที่นี่...</p>
    </div>
  );
};
